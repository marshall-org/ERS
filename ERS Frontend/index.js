let loginButton = document.evaluate("", document, null, XPathResult.ANY_TYPE, null).iterateNext();
let usernameField = document.evaluate("", document, null, XPathResult.ANY_TYPE, null).iterateNext();
let passwordField = document.evaluate("", document, null, XPathResult.ANY_TYPE, null).iterateNext();